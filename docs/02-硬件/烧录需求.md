# 食刻 · 硬件烧录与固件需求（合并版 v2）

> 版本：v2.1 ｜ 更新：2026-08-23（后端接口已实现、平板 v1 固件已落地、Token 配置必改项）
> 来源：由《硬件烧录需求文档-AI硬件平台》（v1）与《固件烧录需求-给烧录AI》（v1）合并而成，原件归档于 `docs/archive/`。
> 目标：T5-E1（SPARKLEIOT T5AI DEV）外置平板 + XIAO ESP32S3 Sense 内模组的构建、烧录与固件功能基线。

---

## 1. 目标硬件

| 部件 | 型号 / 规格 | 角色 |
|---|---|---|
| 外置平板 | **T5-E1（SPARKLEIOT T5AI DEV）**：ST7789 240×320 触控彩屏 + CST816X 触控 + GC2145 DVP 摄像头 + 内置音频 codec/功放 + microSD | 门外交互：待机提醒、放入/取出选择、结果显示 |
| 内模组 | **Seeed XIAO ESP32S3 Sense**（8MB PSRAM，OV3660 摄像头 + PDM 麦克风 + microSD 槽） | 门内感知：扫码触发、条码解码、视觉兜底、语音播报、事件检测 |
| 音频（内模组） | MAX98357A（I2S）+ 3W 喇叭 | “叮 + 名称”播报 |
| 外设（内模组） | 光敏（门开检测）、KY-009 补光、轻触按键 | 事件触发与扫码触发 |

> T5-E1 板载摄像头已确认可用（GC2145 DVP），**外挂 XIAO Sense 不再用于平板侧**；XIAO Sense 仅作内模组。

## 2. 平台与工程环境

| 项 | 外置平板（T5-E1） | 内模组（XIAO Sense） |
|---|---|---|
| 工具链 | TuyaOpen IDE + TuyaOpenSDK，板卡 `SPARKLEIOT_T5AI_DEV` | Arduino IDE 2.x + esp32 包 ≥2.0.8，板卡 `Seeed XIAO ESP32S3` |
| 关键库 | SDK 自带 lwip / HTTP client | esp32-camera（随板卡包内置）、WiFi/HTTPClient；条码库 ZXing 已接入（micro-zxing → Arduino 库，见 [Arduino环境与条码库接入](../03-软件/Arduino环境与条码库接入.md)） |
| 工程 | TuyaOpen 项目 `projects/copy`（app_xzd.c 已 v1 化） | `firmware/inner-module/`（02/03/04/05/D2 示例；04 条码解码已实现） |
| 环境状态 | ✅ 已装，v1 编译通过 | ✅ 已装（2026-08-23）：arduino-cli + esp32 2.0.14 + ZXing + C++17，4 示例编译通过 |

## 3. 固件功能需求

1. **待机态**：从云端 / 本地拉取库存，外置平板低功耗待机（PIR 接近唤醒 + 30s 自动熄屏，对齐 ADR-003），亮屏显示临期 / 过期置顶列表；
2. **触摸选择**：点击屏幕弹出【放入】【取出】——动作由**外置平板触屏**选择，经云端下发内模组（见 §5.2）；
3. **扫码（主路径，内模组）**：条码本地解码优先（ZXing，ms 级）→ 查库播报；无码 / 解码失败 → 视觉兜底（本地 YOLO 品类 ≤500ms）；检测到条码但解码失败 → 语音引导“请转一下条码”（<2s）；
4. **拍照**：内模组 OV3660 抓帧（条码用灰度帧，上传用 JPEG，640×480 质量 ~50–70）；平板 GC2145 仅用于安装自检 / 拍照校准，不参与日常扫码；
5. **结果反馈**：语音播报“叮 + 品名 / 品类名”，外置平板同步显示品名、日期时间、保质期 / 到期日、建议容器；
6. **库存变更**：连续扫无需逐件确认；关门后事件序列结算，差异进入汇总“待确认”，用户一键确认或逐条修正（对齐设计基线 §3.3–3.4）；
7. **声音反馈**：识别成功“叮 + 名称”，失败引导“请转一下条码 / 没看清”，过期警示音；支持夜间静音（屏幕显示 + 短促提示音）；
8. **断网降级**：外置平板显示本地最近库存 + “网络不好”；内模组本地条码 / 视觉识别 + 离线队列，恢复联网后补传（0 丢失、幂等去重）。

## 4. 交互流程（放入 / 取出）

### 4.1 待机页

- 屏幕显示两个大按钮：**【放入】** 与 **【取出】**（可叠加临期 / 过期提醒）。

### 4.2 放入 / 取出流程

1. 外置平板点【放入】/【取出】→ 云端下发动作（`POST /api/device/task`）→ 内模组进入扫码待命；
2. 用户在门口把食品条码对准内模组镜头（或让食品经过镜头）——条码本地解码优先，无码走视觉兜底；
3. 解码 / 识别成功 → 语音播报“叮 + 品名 / 品类名”，连续多件无需逐件确认；
4. 关门 → 事件序列结算（语音清单 vs 事件识别交叉验证）→ 无差异静默入账；有可疑项进入汇总“待确认”（外置平板亮起 + App 首页入口）；
5. 一键确认或逐条修正 → 提交云端 → 回待机页。

> 注：HW-v0 演示版为「平板拍照预览 → 3 秒倒计时 → 归零拍照 → 上传云端 → 屏幕确认」，仅作概念 demo；HW-v1 主路径为内模组条码优先 + 语音 + 关门结算。

### 4.3 失败与兜底

- 上传失败 / 超时：“网络不好，请重试”，不丢本次动作，可重拍；
- 低置信：“没看清，请重新对准再拍”，不写入库存；
- 断网：显示本地最近一次库存与提醒 + “网络不好，先不扫描”；内模组断网入队、恢复重传。

## 5. 云端 API 契约

### 5.1 扫描

`POST https://shike.live/api/scan`（multipart/form-data）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `image` | JPEG | 640×480，质量 ~50–70 |
| `deviceId` | string | 内模组 `shike-xiao-001`；平板 `xzd-t5e1-001` |
| `action` | string | `in`（放入）/ `out`（取出） |
| `container` | string | 冰箱 \| 零食柜 \| 药盒 \| 调料柜 \| 主食柜 |
| `timestamp` | string | `YYYY-MM-DD HH:mm:ss`（NTP 校时，北京时间） |
| `requestId` | string | 幂等键（可选，重试不重复入库） |

响应：`{"code":0,"name":"...","scannedAt":"...","expiryDate":"...","daysLeft":2,"suggestedContainer":"冰箱","confidence":0.92,"keepDays":3,"stockTotal":12}`；`code!=0` 或 HTTP 非 200 按失败处理。

### 5.2 设备指令下发（平板 → 内模组 · ✅ 已实现，2026-08-23）

- `POST /api/device/task`：平板选择动作后调用 `{deviceId, action}`，云端保存“当前动作”；
- `GET /api/device/task?deviceId=...`：内模组扫码前取动作，`action=null` 时默认 `in`；
- 详细契约见 `docs/03-软件/后端对接说明.md` §9。

### 5.3 内模组结果同步（平板轮询 · ✅ 已实现）

`GET /api/scan/latest?deviceId=...`：外置平板等待扫码页每 2–3s 轮询内模组最近扫码事件（name/action/expiryDate/daysLeft/suggestedContainer/stockTotal）；无事件 `data=null`。见 §9.1。

### 5.4 关门结算批次（✅ 已实现）

- 云端按"开门窗口"（相邻扫码间隔 ≤10 分钟）自动结算生成 `SummaryBatch`，见 §9.3；
- `GET /api/summary/pending` + `POST /api/summary/confirm`：平板汇总页已接入（进入拉取 / 确认提交）；
- `POST /api/summary/submit`：内模组关门补充未识别 / 中途取出并立即结算，**固件端待接**（§9.4）。

## 6. 构建与烧录 SOP

### 6.1 外置平板（T5-E1 · TuyaOpen）

```bash
cd /Users/lwl/TuyaOpenIDE/TuyaOpenSDK && . ./export.sh
"$OPEN_SDK_ROOT/tools/tyutool/tyutool_cli" --plain list-ports --json   # 先识别端口
"$OPEN_SDK_ROOT/tools/tyutool/tyutool_cli" --plain write -d t5 \
  -f <工程>/source/embedded/dist/<工程>_QIO_<版本>.bin \
  -p <list-ports 输出端口> -b 460800
```

- 产物：`QIO`（全量，烧录主用）/ `UA`（升级包）/ `UG`（备用），**三者不混用**；
- **端口每次插拔会变**，先 `list-ports` 再烧；T5 双串口，失败换另一个口。

### 6.2 内模组（XIAO · Arduino IDE）

1. 安装 esp32 包 ≥2.0.8，板卡选 `Seeed XIAO ESP32S3`；
2. 装条码库 ZXing 并按文档配 C++17（[步骤](../03-软件/Arduino环境与条码库接入.md)）；
3. 打开 `firmware/inner-module/` 下示例，先改各文件夹内 `config.h`（WiFi / 设备号 / 容器 / 设备 Token）；
4. 选择 USB 端口上传；**端口丢失 / 上传失败 → 按住 BOOT 再插 USB** 进 BootLoader 重传。

> 设备 Token：后端 `pnpm db:devices` 生成后，平板写入 `app_xzd_cfg.h` 的 `XZD_DEVICE_TOKEN`，内模组写入各示例 `config.h`；两台设备 Token 各自独立。

### 6.3 联调前置检查（硬件到货后）

1. 两台设备均烧录完成、串口日志无报错；
2. 平板待机页显示真实库存（device/state 摘要）；
3. 平板点【放入】→ 后端 `device/task` 生效 → 内模组 GET 到动作；
4. 内模组扫码 → 平板结果页同步；关门后平板汇总页出现待确认批次 → 确认后消失。

> 真机联调尚未执行（硬件未到）；代码层契约已对齐、各自编译/类型检查通过。
3. 选择 USB 端口上传；**端口丢失 / 上传失败 → 按住 BOOT 再插 USB** 进 BootLoader 重传。

## 7. 引脚表

### 7.1 外置平板（T5-E1 板载）

| 外设 | 连接 |
|---|---|
| 显示 ST7789 240×320（SPI0） | SCK=44、MOSI=46、CS=45、DC=47、RST=6、BL=9 |
| 触控 CST816X（I2C1） | SCL=20、SDA=21、RST=23 |
| 摄像头 GC2145（DVP） | 控制 I2C0：SCL=0、SDA=1；24MHz MCLK；DVP 数据总线平台固定 |
| 音频 | 内置 codec + 功放，spk_en=GPIO7（高有效） |
| 按键 | GPIO8（低有效，下降沿中断） |
| microSD（SDIO MODE1） | CLK=14、CMD=15、D0–D3=16~19 |
| UART0 | TX=11、RX=10（115200） |

### 7.2 内模组（XIAO ESP32S3 Sense 官方引脚）

| 外设 | 连接 |
|---|---|
| 摄像头 | XCLK=10、PCLK=13、VSYNC=38、HREF=47、SIOD=40、SIOC=39、D0–D7=15/17/18/16/14/12/11/48 |
| PDM 麦克风（板载） | DATA=41、CLK=42（勿占用） |
| 用户灯（板载） | GPIO21 |
| SD 卡（板载，冲刺期不用） | CS=3、SCK=7、MISO=8、MOSI=9 |
| I2S 功放 MAX98357A | BCLK=5、LRC=6、DIN=7 |
| 按键 / 光敏 / 补光 | GPIO2 / GPIO1 / GPIO3 |

> ⚠️ GPIO3/7/8/9 为 SD 卡引脚；**冲刺期不使用 SD 卡**才可复用作 LED/音频，启用 SD 前需调整引脚方案。完整配置见 `firmware/inner-module/内模组-00-共享配置/config.h`。

## 8. 字体与字符集（屏显约束，必读）

> 屏幕是 240×320 低分辨率小屏，中文显示**必须用点阵字库，禁止矢量 TTF**。

| 用途 | 点阵规格 |
|---|---|
| 中文正文 / 按钮 / 结果页 | **16×16 GB2312 点阵字库（HZK16）**，原样点对点输出，不缩放 |
| 数字 / 字母 / 符号 | 8×16 或 16×16 ASCII 点阵 |
| 汇总 / 待确认列表（放入 X / 取出 Y / 待确认 N） | 16×16 点阵，**整数倍（×2 / ×3）最近邻放大**；倒计时大数字仅 HW-v0 演示保留 |

硬性约束：

1. **禁止 TrueType(.ttf) 等矢量字体**与实时缩放渲染；
2. **禁止抗锯齿、灰度、亚像素渲染**：纯 1-bit 二值点阵，字符像素严格对齐网格；
3. **字符集限定 GB2312 简体中文 + ASCII**，固件所有界面文案必须在字库字符集内，避免缺字乱码；
4. 倒计时“半透明”仅指位图字形 + 整数 alpha 混合，字形本身仍为点阵。

## 9. 验收标准

- [ ] 待机页显示【放入】【取出】两个按钮，临期 / 过期提醒置顶；
- [ ] 点【放入】/【取出】→ 云端下发 → 内模组条码本地解码（或视觉兜底）→ 语音播报“叮 + 名称”；
- [ ] 连续扫多件无需逐件确认；重复扫同一件有防重提示；
- [ ] 关门结算：云端自动窗口结算生成批次；平板汇总页拉取真实 pending 并可确认 / 逐条修正；
- [ ] 上传失败有明确提示且可重试；断网内模组本地识别 + 队列补传（0 丢失）；
- [ ] 内模组扫码 → 云端 → 平板刷新闭环（双模组同步）；
- [ ] Wi-Fi 连接涂鸦云成功（PID `pf1xhzdr2zbc2nhw`、授权码有效）。

## 10. 注意事项

- 平板端口每次插拔会变：先 `list-ports` 再烧，不要写死端口；
- T5 是双串口设备：烧录用主口，失败换另一个口重试；
- QIO 为全量镜像，UG / UA 不混用；后续升级用 UA；
- 平板烧录前必改：`XZD_WIFI_SSID/PASS`、`XZD_DEVICE_TOKEN`（后端生成后填写）；
- 内模组四份示例（02/03/04/05/D2）的帧管线规则见 `firmware/README.md`；
- 产品名：**食刻**（食刻在场，不浪费每一份食材），固件屏显文字同步。
