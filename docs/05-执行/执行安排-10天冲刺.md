# 食刻 · 一周半极限冲刺执行安排（10 天）

> 版本：v2.0 ｜ 创建：2026-08-21
> 期限：2026-08-21 → 08-31（D0–D10）
> 目标：**功能全量验证机**——双模组、扫码/视觉、语音、事件、同步、汇总全部可演示可测试，架构与成品一致。
> 诚实边界：量产工程（定制 PCB / 认证 / 试产）物理上无法压缩进 10 天，冲刺后按 §6 后置 2–3 个月；**冲刺版证明功能与架构，量产版在其上工程化**。

---

## 0. 进度快照（2026-08-23）

> 提前完成即打勾；未打勾 = 尚未确认。

| 里程碑 | 状态 |
|---|---|
| D0 采购 / 环境 | ✅ BOM v4.0 核心件已下单（实付约 ¥177）；Arduino esp32 2.0.14 + ZXing + C++17 装好，4 示例全部编译通过 |
| D1 硬件到货上电 | ⏳ 待硬件到货确认 |
| D2 M4 拍照上云 | ✅ 内模组-D2 示例跑通（拍照 → 上传 → 识别） |
| D3 M3 语音播报 | ✅ 内模组-02 示例编译通过（MAX98357A） |
| D4 M6/M7 事件检测 | ✅ 内模组-03 示例编译通过（灰度帧差事件检测） |
| D5 T5-E1 平板 v1 | ✅ 外置平板 v1 门外交互落地（状态机/页面/net，编译通过；汇总页接真实 pending/confirm） |
| D6 后端契约 + 联调 | ✅ task / scan-latest / summary 接口已实现（契约 v3）；⏳ 双模组实体联调、内模组 `summary/submit` 时机待完成 |
| D7 M2 条码本地解码 | ✅ 内模组-04 示例编译通过（ZXing，ms 级） |
| D8 06 条码+YOLO 示例 | ⏳ 已列入 D8 待办 |

---

## 1. 按天计划（D0–D10）

| 天 | 日期 | 任务 | 验收 |
|---|---|---|---|
| D0 | 8/21 | ✅ 采购加急（同城现货/京东次日达/立创）；装 Arduino + TuyaOpen；**开始 EXP-11 手机拍数据** | 下单完成；环境可编译；当天拍 ≥50 张 |
| D1 | 8/22 | 硬件到货 → 内模组拼接（XIAO Sense + 扩展板 + 功放）→ 上电 | M1 摄像头出图 |
| D2 | 8/23 | ✅ M4 拍照 → shike.live 云端识别；✅ T5-E1 平板固件起步 | 返回品名/保质期 |
| D3 | 8/24 | ✅ M5 按键闭环 + M3 语音播报（MAX98357A） | 一按一响一结果 |
| D4 | 8/25 | ✅ M6/M7 事件检测（运动检测 + 放入/取出候选） | 串口事件日志正确 |
| D5 | 8/26 | ✅ T5-E1 待机/库存/汇总 UI + HTTPS GET；双模组云端同步 | 平板显示库存 |
| D6 | 8/27 | ⏳ 双模组联调：扫码 → 云端 → 平板；汇总待确认；断网队列（后端契约 v3 已就绪） | D1–D3 通过 |
| D7 | 8/28 | ✅ M2 条码本地解码（ZXing）；补测项 | 条码号输出 |
| D8 | 8/29 | 本地 YOLO 尝试（**06 示例**：条码+YOLO 同帧并行，RGB565 管线；预训练轻量模型板端跑通即可，不承诺 mAP）；14 项测试记录 | 测试表填完 |
| D9 | 8/30 | 全功能演示脚本 + Demo 操作说明；问题修复 | 演示可跑通 |
| D10 | 8/31 | 最终验收：核心链路 + 事件 + 汇总 + 离线全过；交付 | **功能全量验证机** |

---

## 2. 三条并行线（互不等待）

| 线 | 负责人 | D0–D10 | 说明 |
|---|---|---|---|
| A 内模组 | 用户 | D1–D9 | 摄像头 → 识别 → 语音 → 事件 → 条码 |
| B 外置平板 | 可并行 | D2–D8 | T5-E1 复用现有 app_xzd.c，不等内模组 |
| C 数据与模型 | 并行 | D0–D10 | 手机拍数据集；本地 YOLO 仅"跑通 demo"，精度后置 |

---

## 3. 压缩策略（并行 / 砍掉 / 替代）

| 类型 | 事项 | 做法 |
|---|---|---|
| 并行 | EXP-11 数据 | 手机拍摄即可，不依赖硬件，每天 50–100 张 |
| 并行 | T5-E1 平板 | 与内模组同时开发 |
| 砍掉（后置） | 定制 PCB、OV5640 AF、认证、试产 | 冲刺后量产工程（§6） |
| 替代 | 本地 YOLO | 冲刺版用"条码本地 + 云端识别"作主链路；本地模型仅尝试跑通 demo，mAP 后置 |
| 替代 | 事件检测 | 先做简单运动检测 + 方向候选，中途取出算法后置 |
| 替代 | 推送三通道 | 先做"设备显示 + 待确认"两通道，推送随 App 后置 |

---

## 4. 风险预警（冲刺期）

| 风险 | 概率 | 应对 |
|---|---|---|
| 物流延误（硬件 2 天内不到） | 中 | 同城现货 / 京东次日达 / 立创现货三路下单；T5-E1 已在手可先干平板线 |
| esp32-camera / 库编译卡住 | 中 | 严格按官方示例；卡住先跑云端路径，本地条码/YOLO 后置 |
| T5-E1 HTTPS GET 未验证 | 低 | D5 第一天就验证 GET + JSON |
| shike.live 国内访问慢 | 中 | 固件超时重试；演示环境用可达网络 |
| 零基础排障耗时 | 中 | 按排障表逐项；每步验收不过不前进 |

---

## 5. 最终验收清单（8/31）

- [ ] 内模组：扫码 → 识别 → 播报闭环（M5）
- [ ] 条码本地解码出号（M2）
- [ ] 事件检测：放入 / 取出候选（M6/M7）
- [ ] 外置平板：库存显示 + 汇总待确认（P2/P3）
- [ ] 双模组：同步 + 断网队列（D1/D3）
- [ ] 语音：叮 + 名称 + 静音（M3）
- [ ] 低光补光、防重（T12/T 相关）
- [ ] 14 项手动测试记录完成

---

## 6. 冲刺后量产工程（后置 2–3 个月）

| 阶段 | 周期 | 内容 |
|---|---|---|
| 工程样机 | 2–4 周 | 定制双模组 PCB、OV5640 AF 驱动、外壳/磁吸/pogo |
| 识别达标 | 2–4 周（与上并行） | EXP-11 全量数据集 → mAP ≥85% + ≤500ms（不达标上协处理器） |
| 认证 | 3–5 周 | PSE / TELEC / FCC |
| 试产 | 2–4 周 | 100–500 台，良率 ≥95% |

> 冲刺版与量产版共用同一套功能规格与架构（[成品规格书](../02-硬件/成品规格书-HW1.md)），冲刺验证通过即量产工程按图施工，不返工。

---

## 7. 今天立即行动（8/21）

> 更新（08-23）：以下 8/21 行动项已基本完成，见 §0 进度快照。

1. [ ] 三路下单（同城 / 京东 / 立创）确保 2 天内到货；
2. [ ] 装 Arduino（espressif esp32 包 ≥2.0.8）+ TuyaOpen；
3. [ ] 手机开始拍冰箱食材（30 类，今天 ≥50 张）；
4. [ ] T5-E1 平板固件改造启动（复用 app_xzd.c，先做库存页）。
