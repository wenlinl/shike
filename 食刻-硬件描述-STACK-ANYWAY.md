# 食刻 · 硬件原型描述（供 STACK ANYWAY 生成原型与烧录代码）

> 用途：交给 STACK ANYWAY 构建硬件原型并生成烧录代码。目标板为 **T5-E1（SPARKLEIOT T5AI DEV）**；若平台组件库中没有该型号，可用「T5AI / BK7258 开发板 + ST7789 触控屏 + GC2145 摄像头」替代模型，生成的代码只需替换板级驱动。

## 1. 产品一句话

「食刻」是一台放在冰箱 / 零食柜旁的食品保质期管理扫描台：用户把食品拿到镜头前按一下按钮，设备拍照上传云端 AI 识别，屏幕显示品名、当天日期时间与保质期；待机时屏幕常显临期 / 过期提醒；库存按容器（冰箱 / 零食柜 / 药盒 / 调料柜 / 主食柜）统计，支持补给 / 消耗双模式。

## 2. 硬件组成（BOM）

| 部件 | 型号 / 规格 | 数量 | 用途 |
|---|---|---|---|
| 主控 + 屏 + 摄像头（单板） | **T5-E1（SPARKLEIOT T5AI DEV）**：ST7789 240×320 触控彩屏 + CST816X 触控 + GC2145 DVP 摄像头 + 内置音频 codec/功放 + microSD | 1 | 主控 + 交互 + 拍照上传云端 |
| 蜂鸣器（可选） | 有源蜂鸣器 3.3V | 1 | 备选提示音（板载音频可替代） |
| 温度传感器（可选） | DS18B20 | 1 | 冰箱温度异常提醒 |
| 门磁（可选） | 干簧管 + 小磁铁 | 1 | 开门亮屏提示（辅助，非主链路） |
| 供电 | USB-C 线 + 5V 充电头 / 充电宝 | 1 | 固定供电，无需电池 |
| 外壳 | 3D 打印（底座 + T5 立架） | 1 | 结构件 |

## 3. 电路连接

### 3.1 T5-E1（SPARKLEIOT T5AI DEV）板载外设引脚

| 外设 | 引脚 |
|---|---|
| 显示 ST7789 240×320（SPI0） | SCK=44、MOSI=46、CS=45、DC=47、RST=6、BL=9（背光高有效） |
| 触控 CST816X（I2C1） | SCL=20、SDA=21、RST=23 |
| 摄像头 GC2145（DVP） | 控制 I2C0：SCL=0、SDA=1；24MHz MCLK；DVP 数据总线平台固定引脚 |
| 音频 | 内置 codec + 功放，spk_en=GPIO7（高有效），16kHz 单声道 + AEC |
| 按键 | GPIO8（低有效，下降沿中断） |
| microSD（SDIO MODE1） | CLK=14、CMD=15、D0–D3=16~19 |
| UART0（调试 / 扩展） | TX=11、RX=10（115200，即板载 USB 串口） |
| DS18B20（可选） | 空闲 GPIO + 4.7kΩ 上拉 |
| 干簧管（可选） | 空闲 GPIO，INPUT_PULLUP + 中断 |

> 相机、屏幕、触控、音频全部板载，**无需外挂 XIAO Sense / 摄像头 / 蜂鸣器**（蜂鸣器仅作备选）。

## 4. 通信协议

### 4.1 板载拍照（T5-E1 单板）

点【扫描】→ 板载 GC2145 抓一帧（640×480，参考 copy 工程：实时预览 + 触控定格）→ 直接上传云端；不再经过外挂相机。

### 4.2 T5-E1 → 云端中继（HTTP）

- `POST /capture`：multipart 上传 image（JPEG VGA）+ deviceId + timestamp + action + container → 返回 items JSON（同 4.1 result 结构）；
- `GET /items`：库存清单（按容器分组，组内按剩余天数升序）；
- `GET /reminders`：提醒列表（临期 / 过期置顶）；
- `POST /items`：确认入库 / 出库。

## 5. 设备状态机（固件逻辑）

```text
IDLE（T5 显示临期/过期提醒，等待触摸）
 → 触摸 → CHOOSE（弹出「补给 / 消耗」两个按钮）
 → 选择 → READY（提示“请把食品放到镜头前”）
 → 按【扫描】 → SCAN（“咔嚓”声 → 板载 GC2145 抓帧 → 组装 JSON → POST /capture → 等待结果，超时 8s）
 → RESULT（T5 显示：品名、当天日期时间、保质期/到期日、建议容器；用户确认 → 库存 +1/-1 → 回 IDLE）
失败 / 超时 → 显示“没看清，再扫一次”，回 READY
断网 → OFFLINE（显示最近库存与提醒 + “网络不好，先不扫描”）
```

## 6. 供电

- T5-E1 单板 USB 5V 供电（推荐 5V / 2A 充电头或充电宝）；
- 不用锂电池（低温冰箱场景不适合）。

## 7. 外壳与装配（原型）

- 底座：T5-E1 摄像头开孔朝前（镜头在板载位置）+ USB 走线孔；
- 立架：屏幕斜 30° 朝用户（T5-E1 屏与摄像头同面，支架把镜头对准物品）；
- 装配：单板 + USB 电源线，无需杜邦线；
- 演示道具：日期贴纸（打印「2026-08-17 到期」标签贴在牛奶盒上）。

## 8. 避坑

- 蜂鸣器不能直接接 GPIO，需三极管驱动；
- 大块内存用 `ps_malloc()`，`loop()` 里不用 `delay()`；
- Strapping 引脚 GPIO 0 / 3 / 45 / 46 不要接外围电路；GPIO 26~37 为内部 Flash/PSRAM 专用，不外接；
- 摄像头 DVP 数据总线为 T5AI 平台固定引脚，不要改线；
- 识别失败不猜日期，提示重扫。
